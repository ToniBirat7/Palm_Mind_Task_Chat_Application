### Root .env.example
# Optional global environment variables. You can copy this file into individual folders
# or use the folder-specific examples.

# MongoDB connection string (MongoDB Atlas or local)
MONGO_URI=mongodb://localhost:27017/palm_mind_db

# JWT secret used to sign authentication tokens
JWT_SECRET=replace_with_a_strong_secret

# Backend server port
PORT=5000

# Frontend URL (helpful for CORS / allowed origins)
CLIENT_URL=http://localhost:5173
