// Related to chats
import type { Request, Response } from "express";

import { Conversation } from "../model/chat.model.js";

export const saveConvo = (req: Request, res: Response) => {
  res.json();
};

export const getConvo = (req: Request, res: Response) => {
  res.json();
};

export const deleteConvo = (req: Request, res: Response) => {};

export const editConvo = (req: Request, res: Response) => {
  res.json();
};
